# 작업일지 2026-03-06 (LBC)

## 오늘 한 일

### 1. Claude Code 글로벌 설정 정비
- CLAUDE.md에 Harness 스킬 자동 적용 규칙 추가 (8개 스킬)
  - memory, session, principles, code-review, agent-browser, ci, 퇴근, hookify
- 메모리 자동 분리 규칙 추가 (전역 + 프로젝트별 전용 메모리 병행)
- session-init + session-memory → session 통합 정리
- 전체 스킬 목록 정리 및 전역 vs 상황별 분류

### 2. 프로젝트별 메모리 분리 작업
- `F:\aiProject\monado-main` 전용 메모리 생성
  - 경로: `~/.claude/projects/F--aiProject-monado-main/memory/MEMORY.md`
- 전역 메모리 경로 목록에 monado-main 1순위 추가
- 전역 메모리 monado 내용 실수로 삭제 → 즉시 복원
- 방침: 전역 메모리와 프로젝트 메모리 양쪽 모두 유지

### 3. 퇴근 보고 시스템 버그 수정 (collect-today.py)
- **핵심 버그**: `decode_project_path()` — 프로젝트 경로 디코딩 실패
  - 원인: `-`를 모두 `/`로 변환하는 단순 치환 방식
  - `monado-main` → `monado/main`, `VUP-251104` → `VUP/251104` (잘못됨)
- **수정**: 비트마스크 brute-force로 모든 `-`/`/` 조합을 시도, 실제 존재하는 경로 반환
- 수정 결과: 10개 프로젝트 중 7개 정상 (나머지 3개는 물리 디스크 미연결)
- 3/5 보고서에 누락된 monado-main 커밋 7개 소급 반영

### 4. Agent Teams / psmux 병렬 실행 조사
- Claude Code의 멀티 에이전트 병렬 실행 방법 조사
- tmux 기반 Agent Teams 시각화 방안 탐색
- Windows 대안으로 psmux (PowerShell multiplexer) 검토
- `claude --agent-teams` 미지원 확인 — 아직 CLI에 내장되지 않음
- psmux 설치 및 테스트 시작

### 5. 퇴근 보고서 소급 작성
- 3/5 보고서 업데이트 (monado 커밋/작업 내용 추가)
- 3/6 보고서 신규 작성
- W10 주간 요약 작성

## 주요 변경사항 (Git)

### C:\Users\USER\work-reports
- 4be1696: 작업일지 2026-03-05 업데이트 + 2026-03-06 신규 + 주간 W10
  - collect-today.py 경로 디코딩 버그 수정 포함

## 해결한 문제
- **collect-today.py 경로 디코딩 버그**: 하이픈 포함 폴더명 (`monado-main`, `VUP-251104`, `xr-unity-samples-main` 등)이 깨지는 문제 → 비트마스크 조합 탐색 `decode_project_path()` 함수로 해결
- **3/5 보고서 monado 누락**: 경로 버그로 monado-main 커밋 7개가 수집 안 되었던 것 → 보고서 업데이트로 해결

## 만든 것 / 배운 것
- `decode_project_path()` — Claude 프로젝트 디렉토리 인코딩을 실제 파일시스템 경로로 역변환
- Claude 프로젝트 디렉토리 인코딩 규칙: `--` = 드라이브 구분자, `-` = 경로 구분자 또는 리터럴 하이픈 (구별 불가 → brute-force 필요)
- Harness 스킬 체계 파악: 35+개 스킬 중 8개를 전역 자동 적용으로 선정
- psmux: Windows에서 tmux 대체로 사용 가능한 PowerShell multiplexer

## 다음에 이어서 할 일
- Monado 핸드 트래킹 실사용 테스트 (XREAL 착용 + 손 인식)
- NRSDK 캘리브레이션 JSON 생성 및 적용
- LiveKit UI/UX 디자인 적용 (Dark Glassmorphism + Bento Grid)
- psmux 설정 완료 및 Agent Teams 스타일 병렬 작업 구축
- NotebookLM 리서치 자료 생성

## 메모
- 전역 메모리와 프로젝트 메모리 양쪽 유지가 사용자 선호 (삭제하지 말 것)
- collect-today.py 수정으로 앞으로 하이픈 포함 폴더도 정상 수집됨
- `claude --agent-teams`는 아직 미지원 — psmux 등 외부 도구로 구현 필요
