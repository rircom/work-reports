# 작업일지 2026-03-06 (LBC)

## 오늘 한 일

### 1. Claude Code 글로벌 설정 정비
- CLAUDE.md에 Harness 스킬 자동 적용 규칙 추가 (8개 스킬)
  - memory, session, principles, code-review, agent-browser, ci, 퇴근, hookify
- 메모리 자동 분리 규칙 추가 (전역 + 프로젝트별 전용 메모리)
- session-init + session-memory → session 통합 정리

### 2. 프로젝트별 메모리 분리 작업
- `F:\aiProject\monado-main` 전용 메모리 생성
  - `~/.claude/projects/F--aiProject-monado-main/memory/MEMORY.md`
- 전역 메모리에서 프로젝트 경로 목록에 monado-main 1순위 추가
- 전역 메모리 monado 내용 실수로 삭제 후 즉시 복원

### 3. 퇴근 보고 시스템 버그 수정 (collect-today.py)
- **핵심 버그**: 프로젝트 경로 디코딩 실패 — `-`를 모두 `/`로 변환하는 단순 치환 방식
  - `monado-main` → `monado/main` (잘못됨)
  - `xr-unity-samples-main` → `xr/unity/samples/main` (잘못됨)
- **수정**: 비트마스크 brute-force로 모든 `-`/`/` 조합을 시도, 실제 존재하는 경로 반환
- 수정 결과: 10개 프로젝트 중 7개 정상 (나머지 3개는 물리 디스크 미연결)
- 3/5 보고서에 누락된 monado-main 커밋 7개 추가 반영

### 4. Agent Teams / psmux 조사
- Claude Code의 멀티 에이전트 병렬 실행 방법 조사
- tmux 대안으로 psmux (PowerShell 기반) 검토
- Windows cmd 환경에서의 시각화 방안 탐색

## 주요 변경사항 (Git)
- (오늘 커밋 없음 — 설정 파일/메모리 변경만)

## 해결한 문제
- **collect-today.py 경로 디코딩 버그**: 하이픈 포함 폴더명 (`monado-main`, `VUP-251104` 등)이 깨지던 문제 → 비트마스크 조합 탐색으로 해결
- **3/5 보고서 monado 누락**: 경로 버그로 monado 커밋이 수집 안 되었던 것 → 보고서 업데이트

## 만든 것 / 배운 것
- `decode_project_path()` — Claude 프로젝트 인코딩을 실제 파일시스템 경로로 역변환하는 함수
- Claude 프로젝트 디렉토리 인코딩 규칙: `--`는 드라이브 구분자, `-`는 경로 구분자 또는 리터럴 하이픈 (구별 불가 → brute-force 필요)
- Harness 스킬 체계: memory/session/principles/code-review/agent-browser/ci/퇴근/hookify

## 다음에 이어서 할 일
- Monado 핸드 트래킹 실사용 테스트 (XREAL 착용 + 손 인식)
- NRSDK 캘리브레이션 JSON 생성 및 적용
- LiveKit UI/UX 디자인 적용
- psmux / Agent Teams 실제 설정 테스트

## 메모
- 오늘이 금요일 — 주간 요약 작성 대상
- collect-today.py 경로 수정으로 앞으로 monado 등 하이픈 폴더도 정상 수집됨
- 전역 메모리와 프로젝트 메모리 양쪽 유지가 사용자 선호
