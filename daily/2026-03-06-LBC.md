# 작업일지 2026-03-06 (LBC)

## 오늘 한 일

### 1. Monado 핸드 트래킹 디버깅 (F:\aiProject\monado-main)
- XREAL Air 2 Ultra 착용 후 손 인식 안 되는 문제 디버깅
- stack-local `xrt_frame_context` → `xreal_hmd` 멤버로 이동 (프레임 컨텍스트 소멸 문제)
- descramble 버퍼 300KB를 스택→힙 할당으로 변경
- ONNX 추론 파이프라인 동작 확인 (947 pairs/12초)
- 4가지 핵심 버그 수정으로 E2E 손 인식 성공:
  1. `CAMERA_ORIENTATION_90` → `CAMERA_ORIENTATION_0` (detection 항상 0이었음)
  2. `bool hand_detection_confidence` → `float` (upstream Mercury 버그)
  3. Confidence threshold 0.92 → 0.50 (XREAL 카메라 특성)
  4. `debug_ptr` null check (OpenCV assertion 크래시)
- wrist position 출력 확인: `pos=(-0.175, -0.434, -0.728)`
- StereoKit에서 핸드메쉬 미표시 문제 추가 디버깅:
  - Mercury normalization: mean=0.5, std=0.25
  - confidence_sum: AVG → MAX 변경
  - 카메라 orientation 환경변수 `XREAL_CAM_ORIENT` 추가
  - Debug 이미지 자동 저장 기능 추가

### 2. LiveKit WebRTC UI/UX 재구축 (F:\aiProject\LiveKit)
- Manus AI가 생성한 UI/UX 가이드 분석 및 적용
- PC 앱 + 웹 클라이언트 UI 개선 작업
- 이슈 디버깅:
  - Runtime ConnectionError (signal connection 실패)
  - PC 버전 창 드래그/최소화/최대화 버튼 겹침
  - 화면공유 시 에러 발생
  - 카메라가 다른 세션에서 점유 중인 문제
- 채팅 UI 개선 (내 메시지 오른쪽, 상대방 왼쪽)
- 화면 레이아웃 개선 (내 화면 작게, 상대방 크게, 다중 균일 보기)
- NotebookLM 기반 계획 수립 후 구현 진행

### 3. Claude Code 글로벌 설정 정비
- CLAUDE.md에 Harness 스킬 자동 적용 규칙 추가 (8개)
  - memory, session, principles, code-review, agent-browser, ci, 퇴근, hookify
- 메모리 자동 분리 규칙 (전역 + 프로젝트별 전용 메모리)
- `F:\aiProject\monado-main` 전용 메모리 생성
- session-init + session-memory → session 통합

### 4. 퇴근 보고 시스템 버그 수정 (collect-today.py)
- **경로 디코딩 버그**: `-`를 모두 `/`로 변환 → 비트마스크 brute-force로 수정
- **세션 수집 버그**: 프로젝트당 20개 제한으로 다른 세션 대화 누락 → 세션별 분리 + 30개로 확대
- 서브디렉토리(subagents/) JSONL 탐색 추가

### 5. 에이전트 병렬 실행 도구 조사
- claude-mux (npm) 설치 — tmux 필요로 Windows에서 사용 불가
- psmux (PowerShell multiplexer) 검토
- `claude --agent-teams` 미지원 확인
- 가시적 멀티 에이전트 시각화 방안 탐색 중

### 6. AI 자동화 도구 조사
- OpenManus: API 과금 필요 → 제외
- agenticSeek: API 과금 필요 → 제외
- 결론: NotebookLM이 현실적 대안 (무료, Claude Max와 병행)

## 주요 변경사항 (Git)

### C:\Users\USER\work-reports
- 4be1696: 작업일지 2026-03-05 업데이트 + 2026-03-06 신규 + 주간 W10
- 0126e1a: 작업일지 2026-03-06 최종본 + 주간 W10 업데이트

## 해결한 문제
- **Monado 핸드 트래킹 E2E**: 4가지 버그 수정으로 XREAL에서 손 인식 성공
- **collect-today.py 경로 디코딩**: 하이픈 폴더명 깨짐 → brute-force 조합 탐색
- **collect-today.py 세션 누락**: 단일 프로젝트 20개 제한 → 세션별 분리 수집
- **LiveKit 카메라 점유**: 다른 세션이 카메라 잡고 있던 문제 디버깅

## 만든 것 / 배운 것
- `decode_project_path()` — Claude 프로젝트 인코딩 역변환 (비트마스크 brute-force)
- Mercury hand tracking 4대 버그 수정 패턴 (orientation, float/bool, threshold, null ptr)
- Mercury normalization: mean=0.5, std=0.25 (z-score 아님)
- XREAL 카메라 최적 orientation: `CAMERA_ORIENTATION_0` (물리적으론 270이지만 모델이 landscape 훈련)
- OpenManus/agenticSeek 모두 API 과금 → 무료 자동화는 NotebookLM이 현실적

## 다음에 이어서 할 일
- Monado 핸드 트래킹 실사용 테스트 (안경 착용 + 손 필요)
- NRSDK 캘리브레이션 JSON 생성 및 적용
- LiveKit UI/UX 안정화 (화면공유, PC 창 드래그, 채팅 색상 구분)
- 가시적 멀티 에이전트 시각화 도구 결정

## 메모
- 전역 메모리와 프로젝트 메모리 양쪽 유지가 사용자 선호
- collect-today.py 수정 2건: 경로 디코딩 + 세션별 분리 수집
- OpenManus/agenticSeek = API 과금 필수, NotebookLM = 무료
- 카메라 orientation: L=0, R=270 경향 (좌우 카메라 최적값이 다를 수 있음)
