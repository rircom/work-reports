# 작업일지 2026-03-05 (LBC)

## 오늘 한 일

### Claude Code 환경 설정
- 권한 설정 최적화 (allowedTools, denyTools 구성)
- ntfy.sh 알림 연동 (작업 완료 시 푸시 알림)
- 플러그인/스킬 구조 파악 및 필요한 스킬 설치
- `/퇴근` 커맨드 GitHub 레포 기반으로 전환 (모든 세션 수집 기능 추가)
- 팀원(AGACHUU, HAA02) collaborator 초대

### LiveKit WebRTC 프로젝트 (F:\aiProject\LiveKit)
- LiveKit 프로젝트 초기 설정 및 GitHub 레포 생성
- WebRTC PC-to-PC 영상통화 구현 시도
- GitHub @claude action 및 ntfy 알림 hook 설정
- OAuth 기반 Claude Code Action 전환
- UI 개선 작업 (채팅, 화면공유, 카메라 접근 이슈 대응)
- 다중 접속 (3명) 테스트 진행
- HTTPS 설정 및 외부 접속 테스트

### NotebookLM 연동
- notebooklm-py 설치 및 Claude Code 연동 시도
- LiveKit UI/UX 트렌드 리서치 자료 생성 계획

### 기타
- cmd 창 관련 이슈 (창 늘리기/줄이기 시 세션 끊김)
- Claude Code CLI 로그인 및 설정

## 주요 변경사항 (Git)

### work-reports (신규)
- 초기 설정: 팀 업무보고 레포 구조 생성
- 경로 범용화: H드라이브 → $HOME/work-reports

### LiveKit
- ce1312a: Initial commit - LiveKit WebRTC + StereoKit XR project
- 6332526: GitHub @claude action + ntfy.sh notification hook
- 07da174: OAuth-based Claude Code Action 전환
- 25402b5: OAuth token 추가

## 해결한 문제
- Claude Code 권한 반복 팝업 → allowedTools 설정으로 해결
- `/퇴근` 커맨드가 현재 세션만 반영하는 문제 → 전체 세션 수집 스크립트 작성
- H드라이브 종속 경로 → $HOME 기반 범용 경로로 변경

## 만든 것 / 배운 것
- `rircom/work-reports` GitHub 레포 (팀 업무보고 자동화)
- `scripts/collect-today.py` (git 커밋 + Claude 세션 로그 수집 스크립트)
- LiveKit 프로젝트 초기 구조
- Claude Code 세션 로그가 `~/.claude/projects/` 아래 `.jsonl`로 저장됨을 확인

## 다음에 이어서 할 일
- LiveKit PC 영상통화 안정화 (카메라, 채팅, 화면공유 수정)
- LiveKit UI/UX 디자인 적용 (Dark Glassmorphism + Bento Grid)
- NotebookLM 연동 완성
- 팀원(AGACHUU, HAA02) 초대 수락 후 /퇴근 공유 테스트

## 메모
- LiveKit은 오프라인(폐쇄망) 환경에서도 사용 가능한 WebRTC 솔루션
- 팀원 설정: `cp ~/work-reports/setup/퇴근.md ~/.claude/commands/퇴근.md` 한 줄
- Claude 세션 로그 경로: `~/.claude/projects/{project-dir}/{session-id}.jsonl`
